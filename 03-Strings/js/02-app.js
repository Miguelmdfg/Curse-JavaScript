//Metodos includes y length

const producto = 'Monitor de 20"';

console.log(producto);

//Conocer la cantidad de letras de la cadena de texto

console.log(producto.length);

//Indexof nos va a decir si en este string llamado producto esta la palabra "monitor"

console.log(producto.indexOf);

//Utilizamos "includes" para validar por medio de un booleano que la palabra solicitada exista dentro del string

console.log(producto.includes('Monitor'));
